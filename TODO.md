
# TODO

## Goal: Fix site (pure HTML showing only)

### Step 1 (JS validity)
- Fix `index.js` smart quotes and JavaScript casing/syntax errors so the script runs.
- Verify: no SyntaxError in console.
  - DONE: JS syntax/casing fixes applied in `index.js`.


### Step 2 (HTML validity)
- Fix `index.html` smart quotes in attributes so images/links/classes work correctly.

### Step 3 (CSS validity)
- Fix `index.css` smart quotes and invalid capitalization so styles apply.

### Step 4 (Manual verification)
- Reload and confirm: styling works, reveal animations work, hover tilt works, request button updates note.

